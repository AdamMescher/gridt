import * as React from 'react';
import styled from 'styled-components';

const SchoolSizeAlertMessage = () => {
  <p>
    school does not have enough students in the subgroup to generate meaningful
    data.
  </p>;
};
