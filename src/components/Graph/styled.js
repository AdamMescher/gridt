import styled from 'styled-components';

const StyledGraph = styled.div`
  flex-grow: 1;
  display: flex;
  justify-content: center;
  .graph-size-error-container {
    flex-direction: column;
    padding: 10px;
  }
`;

export default StyledGraph;
